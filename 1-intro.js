// Globals   No Window
// __dirname    - name to current directory
// __filename   - file name
// require  - function to use module (CommonJS)
// module   - info about current module (file)
// process  - info about env where the program is being executed

const name = "tarjan";
const sayName = (name)=>{
    console.log("hi" + name);
}

sayName(name);